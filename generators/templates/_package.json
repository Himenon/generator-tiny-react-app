{
  "name": "<%= projectName %>",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cross-env NODE_ENV=development ts-node develop.ts",
    "build": "cross-env NODE_ENV=production ts-node build.ts",
    "serve": "serve dist",
    "format": "eslint --fix -c .eslintrc.json '**/*.{ts,tsx}'"
  },
  "devDependencies": {}
}
